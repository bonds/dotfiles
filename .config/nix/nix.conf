# Reference only — these are managed by the nix.nix module on each machine.
# See modules/nix.nix (shared), hosts/accismus/configuration.nix (darwin),
# and modules/nixos-common.nix (NixOS).
experimental-features = nix-command flakes
warn-dirty = false
max-jobs = auto
